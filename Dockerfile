# Виберіть базовий образ
FROM python:3.10-slim

# Встановіть робочу директорію всередині контейнера
WORKDIR /app

# Скопіюйте залежності вашого проекту
COPY requirements.txt .

# Встановіть залежності
RUN pip install --no-cache-dir -r requirements.txt

# Скопіюйте інші файли вашого додатка в робочу директорію
COPY . .

# Експортуйте порт, на якому буде працювати ваш додаток
EXPOSE 8000

# Команда для запуску додатку
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
